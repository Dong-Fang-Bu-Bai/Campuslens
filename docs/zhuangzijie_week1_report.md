# 第一周工作周报

## 基本信息

| 项目 | 内容 |
|------|------|
| 姓名 | 庄子杰 |
| 模块 | M5 用户反馈纠错与检索记录统计 |
| 日期 | 2026-05-19 — 2026-05-24 |
| 分支 | `feature/m5-feedback-zhuangzijie` |

## 本周完成工作

### 1. 环境搭建

- 克隆项目仓库到本地开发环境。
- 配置 MySQL 数据库连接，初始化 `database/schema.sql` 基础表结构。
- 安装后端 Spring Boot 开发环境（JDK 17 + Maven）。
- 安装前端 Vue 3 + Vite 开发环境（Node.js 20 + pnpm）。
- 验证后端健康检查接口 `GET /api/health` 正常响应。
- 验证前端开发服务器正常启动，原型页面可访问。

### 2. 分支管理

- 从 `dev` 分支切出个人开发分支 `feature/m5-feedback-zhuangzijie`。
- 确认分支切换正确，工作目录清洁。

### 3. M5 数据库设计

- 编写 `database/m5_init.sql`，包含以下两张表：
  - **search_records**（检索记录表）：存储用户图片检索记录，字段包括 ID、用户 ID、图片路径、地标 ID、状态和时间戳。
  - **user_feedback**（纠错反馈表）：存储用户纠错反馈数据，字段包括 ID、用户 ID、关联检索记录 ID、图片路径、预测地标 ID、确认地标 ID、反馈类型、备注、状态和时间戳。
- 表结构遵循项目命名规范（数据库 snake_case，接口 camelCase）。
- 设置合理的外键约束和默认值。

### 4. 用例规约编写

- 编写 `docs/uc_feedback.md`，覆盖"用户反馈纠错"完整用例。
- 定义了基本流（查看结果 → 点击纠错 → 选择正确地标 → 系统保存）六个步骤。
- 补充了三个备选流（识别正确、不确定、表单校验失败）和两个异常流（网络失败、服务端异常）。
- 明确了五条业务规则和涉及接口、数据表。

## 遇到的问题

- 暂无阻塞性问题。

## 下周计划

1. 实现 `POST /api/feedback` 接口，完成反馈数据持久化。
2. 对接前端反馈表单，联调纠错提交流程。
3. 编写反馈相关单元测试。
4. 配合 M1/M4 联调完整的"检索 → 结果展示 → 反馈纠错"主流程。

## 备注

- 本周交付物为设计文档，代码实现将在第二周展开。
- 当前不执行 `git push`，待组长确认后统一推送。
